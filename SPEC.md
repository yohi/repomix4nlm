# 技術仕様書: Repomix NotebookLM 分割ラッパー (`repomix-nlm-splitter`)

本仕様書は、`repomix-nlm-splitter` の内部仕様、アーキテクチャ、データフロー、および各種アルゴリズム（チャンク分割、命名規則、エラーハンドリングなど）の詳細をまとめたドキュメントです。

## 1. 全体アーキテクチャ

本ツールは Repomix のプログラム用 API（`pack`, `searchFiles`, `mergeConfigs`）を内包して動作するラッパー CLI です。
責任と役割が以下のモジュールに分離されています。

- **`index.ts`** (オーケストレーター):
  CLI の入力受付、一時ディレクトリ（`os.tmpdir()` 内）の作成、クローンの実行、ブランチ取得、出力ディレクトリの決定、チャンク分割処理の呼び出し、パック出力の実行、および `finally` ブロックによる一時クローンの強制削除を行います。
- **`cli.ts`** (引数パース):
  入力された CLI 引数の妥当性をチェックし、パース結果を返却します（純粋関数）。
- **`parser.ts`** (URL/パス解析):
  Git URL から `owner/repo` を抽出したり、ブランチ名や日付から安全な出力先ディレクトリ名を構築したりします（純粋関数）。
- **`git.ts`** (Git操作):
  リポジトリの shallow clone（`--depth 1`）やブランチ名の取得を担います。
- **`chunker.ts`** (チャンク分割コア):
  ファイルの走査、語数計測、ビンパッキング、およびチャンク命名を実装した純粋ロジックです。
- **`repomixRunner.ts`** (Repomixアダプタ):
  チャンクごとの `repomix` 設定（XMLスタイル、`.txt` 拡張子明示指定など）を構築し、`pack` API を実行します。
- **`constants.ts`** (定数定義):
  デフォルト閾値（`360000` 語）や除外すべきバイナリ拡張子の定義を保持します。

---

## 2. データフロー

入力された Git URL から最終的なファイル出力までの処理の流れは以下の通りです。

```text
入力: <git-url> [--branch name] [--threshold 360000] [--out-dir .] [--compress] [--keep-tmp] [--no-enable-security]
   │
   ▼
[1] cli.ts       ─ 引数パース（各オプション、Git URLの抽出と検証）
   │
   ▼
[2] parser.ts    ─ Git URL解析 → owner / repo の抽出
   │
   ▼
[3] git.ts       ─ os.tmpdir() 内に一意な temporary ディレクトリ作成 → git clone（--depth 1 [--branch name]）
   │             ─ git rev-parse --abbrev-ref HEAD によるカレントブランチ名取得
   │             ─ 出力ディレクトリ名確定: {owner}-{repo}-{branch}-{YYYYMMDD}
   │
   ▼
[4] chunker.ts   ─ repomix.searchFiles() を呼び出して対象ファイルを列挙
   │               （.gitignore / デフォルト除外を継承し、バイナリ/アセット拡張子を追加除外）
   │             ─ 階層構造（パス辞書順）でファイルをソート
   │             ─ 各ファイルの中身を読み込んで概算語数（空白/改行 split）を計算
   │             ─ 貪欲ビンパッキングにより、語数が上限を超えないようグループ（Chunk[]）に分割
   │             ─ 共通親ディレクトリに基づくチャンク代表名の命名と衝突回避の連番付与
   │
   ▼
[5] repomixRunner.ts ─ チャンクごとに pack(rootDirs, config, ..., explicitFiles) を呼び出し
   │                   （config.output.filePath に .txt パスを明示し、拡張子自動調整をスキップ）
   │                   → XML 形式の .txt ファイルを出力ディレクトリに書き出し
   │
   ▼
[6] index.ts (finally) ─ 一時ディレクトリを強制削除（fs.rm recursive, force）
```

---

## 3. チャンク分割アルゴリズムと命名規則

### 3.1 階層構造ソート
対象ファイルを Repomix の `sortPaths()` に基づき、ディレクトリ階層順・パス辞書順に並べ替えます。これにより、同一ディレクトリ内のファイルが極力同じチャンクに隣接してパッキングされます。

### 3.2 貪欲ビンパッキング (Greedy Bin Packing)
各ファイルの語数を空白と改行で分割したトークン数として算出し、以下のルールでグループ化します。

- 閾値（`THRESHOLD`）: デフォルト `360,000` 語（NotebookLM の 500,000 語の制限の 72% に設定。XML タグのオーバーヘッド等を考慮し、約 28% の安全マージンを確保）。
- **特例（巨大ファイル）**:
  単一のファイルサイズが `THRESHOLD` を超えている場合、エラーにせず**そのファイル単体で独立した 1 チャンク**として出力します。
- **通常パッキング**:
  ファイルを順次チャンクに詰め込み、次のファイルを追加すると `THRESHOLD` を超える場合に、現在のチャンクを確定させて新しいチャンクを開始します。
- **`--compress` 有効時の見積もり挙動（安全側の過分割）**:
  語数カウントおよびチャンク分割の判定は常にファイルの**生語数**ベースで行われます。そのため、`--compress` 指定時は Tree-sitter によって実際の出力語数が削減されるのに対し、チャンク分割は生語数ベースで過大評価して行われます。結果として、本来1チャンクに収まるファイル群が複数チャンクに分かれる「安全側の過分割」が発生しますが、これは NotebookLM の制限を確実に下回るための許容された設計動作（安全マージンの強化）です。

### 3.3 チャンクの代表名と衝突回避
各チャンクの出力ファイル名は、以下の命名規則に従います。

1. **共通親ディレクトリ名**:
   チャンク内ファイルの「共通親ディレクトリ（common path prefix）」を算出し、パス区切りの `/` を `-` に置換した文字列（例: `src-core-metrics`）をベース名とします。
2. **単一ファイルのチャンク**:
   単一の巨大ファイルが独立チャンクになった場合は、ファイルパス全体をハイフン化した文字列（例: `dist-bundle.js`）をベース名とします。
3. **ルート直下**:
   共通親ディレクトリが存在しない（ルート直下ファイル群の）場合、`root` をベース名とします。
4. **衝突回避の連番サフィックス (`-chunkN`)**:
   複数のチャンクが同一のベース名を持つ場合（同一ディレクトリ内のファイル群が閾値超えで複数に分割された場合など）、ベース名の末尾に自動的に `-chunk1`, `-chunk2` などのサフィックスを付与して一意性を確保します。

### 3.4 Repomix mergeConfigs による拡張子維持の仕組み (検証済み事実)
`repomix` の `mergeConfigs(cwd, fileConfig, cliConfig)` は、`output.filePath` が明示的に設定されている場合、`configLoad.ts` 内の `filePathExplicitlySet` 分岐により自動的な拡張子調整を行わない仕様となっています。この挙動を利用することで、出力スタイルに `style: 'xml'` を指定しても、明示された `.txt` 拡張子が上書きされずに保持されるように設計されています。

### 3.5 セキュリティスキャンの制御仕様
安全なリポジトリ解析を提供するため、本ツールはデフォルトで Repomix のセキュリティスキャン（秘密情報や認証情報の検知など）を有効化した状態で `pack` API を呼び出します。
CLI 引数において `--no-enable-security` オプションが渡された場合、`RunChunkOptions.enableSecurityCheck` を `false` に設定し、Repomix 内部の `security.enableSecurityCheck` 設定を `false` にオーバーライドします。これにより、信頼できるリポジトリに対する処理速度の向上を図ることができます。

---

## 4. 除外拡張子設定

バイナリファイルやフォント、マルチメディアなどの非テキストアセットは、語数計算やパックの対象から完全に除外するため、Repomix の `ignore.customPatterns` に glob パターン（`**/*.{ext}`）として登録します。

**完全除外する拡張子の一覧:**
- **画像/アイコン**: `svg`, `png`, `jpg`, `jpeg`, `gif`, `ico`, `webp`, `bmp`
- **ドキュメント**: `pdf`
- **アーカイブ**: `zip`, `tar`, `gz`, `tgz`, `rar`, `7z`
- **フォント**: `woff`, `woff2`, `ttf`, `eot`
- **メディア**: `mp4`, `mov`, `mp3`, `wav`

また、Repomix の `useDefaultPatterns: true`（`node_modules` や `.git` などの除外）および `useGitignore: true` も併せて適用されます。

---

## 5. エラーハンドリングとクリーンアップ

- **Git 認証エラーの検出**:
  `git clone` 実行時の `stderr` を監視し、認証失敗（`permission denied`, `authentication failed` 等）を検出した場合、ユーザーに対してSSHキーの設定やトークンの確認を促す明示的なメッセージを出力して終了します。
- **一時ディレクトリのクリーンアップ**:
  処理の途中でエラーがスローされた場合も含め、`finally` 句において `fs.rm(tmpDir, { recursive: true, force: true })` を呼び出し、作成した一時作業ディレクトリを確実に削除します（`--keep-tmp` が指定されている場合を除く）。
- **個別パックエラー**:
  いずれかのチャンクで Repomix の `pack()` が失敗した場合は、エラーを出力して全体の処理を中断し、部分的な不完全出力を抑止します。

---

## 6. テスト戦略

本ツールの各機能モジュールは、開発の迅速化と品質確保のため、モックを効果的に利用した単体テスト（TDD）および実機・ネットワークに依存した統合テスト/スモークテストに切り分けてテストが設計されています。

- **`parser.test.ts` (URL/パス解析の検証)**:
  - 外部ネットワークへのアクセスを行わない純粋関数であるため、GitHubやBitbucket、HTTPS/SSH/認証情報入りなど各種 Git URL に対するパース処理やサニタイズ処理を詳細に単体テストします。
  - 日付による出力ディレクトリ名生成は、テスト実行タイミングに依存しないよう `Date` オブジェクトを外部から注入して固定値を検証します。
- **`chunker.test.ts` (チャンク分割コアの検証)**:
  - 本ツールの最も核心となるアルゴリズム部分です。語数計算、ビンパッキングの境界値、閾値超え巨大ファイルの独立処理、共通親ディレクトリのパス解決、およびチャンク命名の衝突回避（`-chunkN` 連番）を詳細にテストします。
  - `buildChunks` のテストにおいては、ファイルシステムへの直接の I/O や `repomix` のファイル検索処理が混ざるのを防ぐため、依存性注入（DI）を利用して `readFile` や `searchFiles` を完全にモック化した状態でロジックのみを検証します。
- **`repomixRunner.test.ts` (Repomix設定/実行の検証)**:
  - `repomix` の `pack()` API は直接呼び出さず、`vi.fn()` によるスパイ・モックを注入してテストします。
  - 主に、渡されたオプション（`.txt` 拡張子明示指定、XMLスタイル、`compress` フラグ、除外パターン、セキュリティスキャン設定など）が正しく Repomix の config オブジェクトにマッピングされて `pack()` に渡されているかをアサーションします。
- **統合・スモークテスト**:
  - Git 操作や実際のクローン処理、最終ファイルシステムへの書き出しは実機依存となるため、単体テストでは行わず、テスト環境下（または `.devcontainer` 内）での実際のリポジトリを用いた CLI 実行（スモークテスト）によって検証します。
